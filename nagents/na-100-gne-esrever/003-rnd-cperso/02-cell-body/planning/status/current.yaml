# Current Agent Status — Runtime State

session:
  id: null
  started_at: null
  user_id: null

active_tasks:
  - task_id: "task-002"
    status: in_progress
    current_step: >
      2026-07-01: task-002.2 COMPLETE — C++ EMV perso engine architecture delivered
      (08-memory/long-term/thales/emv-engine-architecture.md). Next: task-002.3 implementation,
      starting Phase P1 (tlv + profile loaders). task-002.1 resolved; open Q5/Q6 resolved; SCP02 confirmed.
      NOTE: implementation delegated to na-005 BRUID agents (task-003); this agent stays planner/review.

  - task_id: "task-003"
    status: in_progress
    current_step: >
      2026-07-13: live MC card (AID A0000000041010) presented for perso. Confirmed two gaps blocking
      script generation. Actions taken as planner/coordinator:
      (1) KEYS-FIRST — filed bureau key request to na-003/007-bnprs-grc-kms
          (its 01-dendrite/inputs/handoff-na005-008-perso-engine-bureau-keys.md): SCP02 ISD KVN=01
          label+KCV + diversification method (default GP 40..4F ruled out; div-data 00002060BB8D509C2020
          => MC-style), plus IMK-AC/SMI/SMC, CVK-A/B, PVK, PIN-TK — labels+KCVs only (PCI).
      (2) Routed impl hand-offs: bruid-dprep task-001.4 (fix MC-ADDONS DGIs:0 grouping bug);
          bruid-iperso task-001.1 annotated (build P5 Sequencer/script-emitter + dry-run on KATs now).
      (3) 2026-07-14: Pointman/MENTA vendor-kiosk integration (v1.3) folded into na-005. MENTA = perso bureau,
          Pointman = kiosk hardware, BRUID = perso backend. dPrep now has TWO input channels: CENTRAL = embossing
          file (Tri-Badge PURE -> cperso) / INSTANT = MENTA DPI (G&D data-provider -> iperso). Part A (frontend) +
          Part B (SOAP/WS-Security/DUI) are ANOTHER team member's; iperso owns only the perso script (BprCardEmv).
          Surfaced a SECOND key domain — the MENTA DEK (AES-256) wrapping the DPI secure block; see key_dependencies.
      (4) 2026-07-14: UAT keys RECEIVED from perso bureau (Resources/UAT_Keys.txt). Verified end-to-end:
          ZMK = P1^P2^P3 (component KCVs 6DB2A2/CCC0E4/E24E54), reconstructed ZMK KCV A85DE2; IMK-AC/SMI/SMC
          decrypted under ZMK -> clear KCV 82E136 (all match bureau). IMK is USABLE for UDK derivation.
          Bureau clarified: for DATA PREP only the IMK is needed; CVK/PVK/PIN-TK are transaction/host-side.
          OPEN (planner to confirm w/ bureau): iCVV/CVV2 supplied-in-DP vs computed (would need CVK); offline
          reference PIN loaded-at-DP vs online/kiosk-set (would need PIN-TK). With ISD key (C277BA/VISA2) +
          IMK (82E136) both in hand, UAT data prep + card perso are UNBLOCKED.
      (5) 2026-07-14: PERSO FULLY VALIDATED + HANDED OFF. Found the correct INSTALL params in the Operas/THALES
          MDA traces (C9 05 0111000105, pkg A0000000180F000001833032, module A0000000180F0000018303, instance
          A0000000041010, priv 12); validated LIVE on card: INSTALL->9000, SELECT->9000 (was 6999), applet SCP02
          (gp --connect), STORE DATA DGI A002->9000. CVK/PVK NOT DP keys; PIN in input. MC_EMV_Perso.spi = the
          port template. Full perso build handed to bruid-cperso (its task-001 + input 2026-07-14-uat-perso-
          handoff.md). Gemalto vendor query SUPERSEDED. mem-014/015.
      (6) 2026-07-15: ENGINE FEATURE-COMPLETE — bruid-cperso delivered the full central perso engine
          (bpr.cpp/src/BprCardEmv/persoengine, 86 tests green). Planner review DONE: the full 41-DGI M/Chip
          Advance STORE DATA stream assembles (plaintext byte-exact vs the Operas trace), full APDU sequence
          (SELECT->INIT UPDATE->EXT AUTH->DELETE->INSTALL[0C]->STORE DATA->SET STATUS) all byte-exact, RSA/ODA
          ICC-cert builder (task-004) + DEK-wrap + live PC/SC driver (over bpr.cpp BprPcSc). Canonical DGI decode
          authored this session: 08-memory/long-term/thales/mc-advance-dgi-map.md (3-class architecture: 26
          verbatim config / 3 TLV records / RSA-cert + DEK-key). See bruid-cperso knowledge.yaml mem-015 for the
          commit trail. task-003 delegation + task-004 ODA now satisfied in-engine.
      Planner role now: 3 EXTERNAL R&D gates remain (below) — none are engine code. Keep design authoritative.
      Remaining external deps: MENTA DEK (instant channel, bruid-iperso, out-of-band) + the 3 gates in open_gates.

# ---- Open R&D gates (engine complete; these are the only blockers to a LIVE central perso) ----
open_gates:
  - id: "visa2-diversification"
    status: RESOLVED   # 2026-07-15
    what: >
      VISA2 static-key diversification — RESOLVED. Ran gp.jar --key-kdf visa2 -d --list on the live UAT card
      (ACR39U): EXT AUTH -> 9000, channel opened. The engine (scp02::visa2_diversify + visa2_session_keys,
      bpr.cpp 181a2ae) reproduces that session's card cryptogram 4E69A8C28F08308D AND host cryptogram
      F602F36D214BAEF5 byte-for-byte (keystore-gated test). KDF = GPPro fillVisa layout (kdd[0:2]||kdd[4:8]||
      F0/0F||keyId). The earlier offline miss used a stale kResp1 vector from a different session/card. The
      SCP02 session + real session DEK are now fully derivable in-engine — real 8000 (UDKs) + 8010 (ICC priv)
      key DGIs unblocked (8010 still needs the per-card ICC RSA key from the ODA path).
    owner: "rnd-cperso (R&D) — DONE"
  - id: "issuer-rsa-key"
    status: UAT-RESOLVED   # 2026-07-15 — test-CA mode (bpr.cpp 39bdacc); real-CAPK chain still needs bureau key
    what: "Real issuer RSA private key for PRODUCTION-CHAIN 9F46 certs. UAT is UNBLOCKED: test-CA mode (self CA signs issuer key -> regenerated 0404; issuer signs ICC -> 0401/0403) gives a self-consistent, terminal-verifiable chain with no external dependency. Only cards that must chain to the bureau's real 0404/CAPK need the real issuer key."
    resolution: >
      NOT a grc-kms key — it is asymmetric issuer PKI held by the PERSO BUREAU (MENTA/Thales), who already
      signed the trace's 9F46 (we hold the matching Issuer PK Cert as DGI 0404). Route to the bureau, not
      grc-kms. Fork: (A) bureau signs 9F46 (we never hold the key) vs (B) bureau provisions the key HSM->HSM
      encrypted into our HSM. UAT SHORTCUT buildable now: self-generated test issuer key + Mastercard TEST
      CAPK + regenerate 0404. Full process + bureau checklist: 07-axon-terminals/deliverables/
      2026-07-15-issuer-rsa-key-process.md (task-004.1).
    owner: "perso bureau (MENTA/Thales) + user (topology decision)"
  - id: "bprpcsc-macos-transmit"
    what: "BprPcSc live SELECT hits 'SCardTransmit: Protocol mismatch' on macOS/ACR39U (ATR reads fine). Backend picks SCARD_PCI_T0/T1 from the stored active protocol."
    resolution: "Fix in BprPcSc backend_pcsclite, or run the live perso on a Windows/Linux POS where that path is exercised."
    owner: "BprPcSc maintainer (shared component)"

# ---- Coordination view: KEY DEPENDENCIES (two domains) ----
key_dependencies:
  domain_A_emv_bureau:
    source: "na-003/007-bnprs-grc-kms (perso bureau); labels + KCVs only (PCI)"
    channel: "both (central + instant)"
    requested_via: "grc-kms 01-dendrite/inputs/handoff-na005-008-perso-engine-bureau-keys.md (2026-07-13)"
    keys:
      - "SCP02 ISD static S-ENC / S-MAC / S-DEK, KVN=01 + card diversification method (the gate) — HELD (C277BA/VISA2)"
      - "IMK-AC / IMK-SMI / IMK-SMC — per-card UDK derivation — RECEIVED (KCV 82E136, validated). KMS labels:
         IVSCI(AC)/IVSTP(SMI)/IVSTV(SMC)/IVSTR(IDN), owner TRANSLINK (per MC_EMV_Perso.spi + GenericCrypto.ini)"
    resolved_2026_07_14_via_MC_EMV_Perso_spi:
      - "CVK-A/B NOT a DP key — ICVV/CVV2/PVV are PARSED from the DP input (pre-computed upstream), never
         computed in perso. Confirms bureau. Drop CVK from the bureau ask for DP."
      - "PVK NOT a DP key — PVV supplied in input (host-side value)."
      - "PIN: pinblock arrives in the DP input under the issuer transport key; perso re-encrypts to the card key
         (SMC) via KMS TranslatePinBlock -> DGI 8201. PIN-TK is a KMS/data-side key, not a card-load key."
      - "ODA/RSA fully handled in-script via KMS (GenerateRSAKeyPair, GetIssuerPKCertificate,
         GenerateICCPKCertificate, GenerateSDAD) — DGI A001 (8F/90/92/9F32/93/9F46/9F47/9F48) + DGI 8010 ICC priv."
    plus_oda: "ODA/RSA = task-004 — DONE in-engine (perso::oda, bpr.cpp b63fdbd): ICC PK Cert builder per EMV
       Book 2 6.4, verified end-to-end offline with a test issuer key. Real 9F46 needs the real issuer RSA key
       (open_gates: issuer-rsa-key)."
  domain_B_data_exchange:
    source: "MENTA (perso bureau, upstream); exchanged OUT-OF-BAND; record LABEL only"
    channel: "INSTANT only (DPI path -> bruid-iperso)"
    keys:
      - "DEK (e.g. MENTA/DEK/DEV_DEK_02) — AES-256-CBC key wrapping the DPI xe:EncryptedData secure block"
    note: >
      DISTINCT from the EMV bureau keys — NOT sourced via grc-kms. Needed by bruid-dprep task-001.6/001.7 to
      decrypt the instant DPI. Central channel (embossing file) needs NO DEK. Tracked in dprep needs_from_user.

queue:
  pending: 0
  in_progress: 2      # task-002 cperso.thales; task-003 delegation/coordination (engine done; 3 external gates open)
  on_hold: 1          # task-001 BprDataPrep
  completed: 1        # task-004 ODA/RSA — delivered in-engine (perso::oda)
  blocked: 0
  # NOTE: cperso ENGINE is feature-complete (86 tests). Remaining blockers to a LIVE perso are the 3
  #       external gates in open_gates (VISA2 capture, issuer RSA key, BprPcSc macOS transmit) — not engine code.

resources:
  active_connectors: []
  memory_usage: null
  tokens_consumed: 0
  tokens_remaining: null
