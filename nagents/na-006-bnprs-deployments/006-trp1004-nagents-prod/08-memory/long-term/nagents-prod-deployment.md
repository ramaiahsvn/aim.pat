# nagents PROD — Deployment Record (TRP1004)

**Deployed:** 2026-07-25/26 · **Account:** ITP `819144294008` · **Region:** `ap-south-2` (Hyderabad)
**AWS profile:** `itp` · **IaC repo:** `bpr0000.bnprs.portal.iaac` (branch `bp_rel`)
**Source of truth:** `bp_rel` (MR !5 + !6 merged). Deploys were direct `terraform apply` (stopgap); proper path = CI-driven.

> Deployed with a locally-downloaded Terraform **1.9.8** (system had 1.5.7 < required 1.6). aarch64/whatever host; TF binary in the session scratchpad.

---

## 1. Platform (nagents-platform/platform/prod)
| Resource | Value |
|---|---|
| tfstate | S3 `nagents-prod-tfstate-819144294008` + DynamoDB lock `nagents-tflock-prod` |
| VPC | `10.61.0.0/16`, 2 AZ (ap-south-2a/2b), 3 tiers; 5 VPC interface endpoints (ECR api+dkr, Secrets, SSM, Logs) |
| ALB | `nagents-prod-alb` — DNS `nagents-prod-alb-1730702192.ap-south-2.elb.amazonaws.com`, ARN `…/app/nagents-prod-alb/df8bce2529fd6fa7`, zone `Z0173938T07WNTVAEPZN`. **HTTP(80) + HTTPS(443)** listeners |
| RDS | `nagents-prod-postgres` (PG 16.14, db.t4g.medium, single-AZ, 50GB), db **`nagents`**, master user `nagents_admin`. Endpoint `nagents-prod-postgres.cv2qcoeai3kc.ap-south-2.rds.amazonaws.com` |
| Redis | `nagents-prod-redis` (cache.t4g.medium ×2, TLS). Primary `master.nagents-prod-redis.uwgdhe.aps2.cache.amazonaws.com:6379` |
| ECS | cluster `nagents-prod-ecs` (Fargate); Service Connect ns `nagents.prod.local` |
| Secrets | `nagents/prod/rds/master-password` (plain), `nagents/prod/redis/auth-token` (plain) — auto-generated by TF |
| ECR | `nagents/{erp, projects, chats, chats-go-api, bnprs-dashboard}` |
| EIPs (bootstrap) | NAT `16.112.108.85`, OpenVPN `16.113.69.83` |
| OpenVPN | **disabled** (`openvpn_enabled=false`) — needs Marketplace sub + admin CIDRs. Its SG `sg-…openvpn-sg` (sg-0f562a30ace513dc9) is RDS-allowed (handy for one-off DB tasks) |
| ALB access logs | bucket `nagents-prod-alb-logs-819144294008` (service-principal policy — ap-south-2) |

**DB schemas (single `nagents` db, schema-per-app):** `erp`, `sprints` (+ `_prisma_migrations` in erp). Created via one-off Fargate `postgres` task (openvpn SG).

---

## 2. Backends (ECS Fargate services, healthy 1/1)
| Service | Image | Schema | Migrations | ALB host rule |
|---|---|---|---|---|
| `nagents-prod-erp-hr-service` | `…/nagents/erp:991b7fc4cfe598aaa5cab25b0b95c4e66aecd954` | `erp` (search_path) | **Prisma** (`prisma migrate deploy`, 83) — binary does NOT self-migrate | `erp-api.prod.itpgateway.com` (80+443) |
| `nagents-prod-projects-backend` | `…/nagents/projects:bdd356edf951087837a9931d7111545b332ae403` | `sprints` | **auto-migrates** on start | `projects-api.prod.itpgateway.com` (80+443) |

- Images **promoted from UAT ECR** (eu-central-1 → ap-south-2) via `docker buildx imagetools create`.
- Secrets: DB/Redis from prod Secrets Manager; **keycloak client secret + SES SMTP creds reused from UAT** (`nagents/uat/{erp-hr-service,projects-backend}` JSON secrets); `INTERNAL_API_TOKEN`/`APP_ENCRYPTION_KEY` freshly generated. **Fresh DBs (no UAT data migrated).**
- Config fixes applied: erp `s3_bucket` → `nagents-prod-hr-documents-…` (was erp-uploads); erp needs `db_password`+`redis_password` vars in addition to `database_url`/`redis_url`; projects `db_name=nagents`+`db_schema=sprints` (was sprints/""); `try()` on `alb_https_listener_arn` in both.

### erp Prisma migration runbook (for future migrations)
1. Clone `bpr0000.bnprs.erp` @ the deployed image commit. Prisma `^5.22`, `prisma/migrations/*`.
2. Build a `prisma migrate deploy` image on **Debian** (`node:20-slim`+openssl) — **Alpine fails** ("Could not parse schema engine response / libssl").
3. Push to prod ECR (`nagents/erp:migrate-prisma522`), run one-off Fargate task (task-def `nagents-prod-erp-migrate`, exec role `nagents-prod-dbinit-exec`, openvpn SG) with `DATABASE_URL=…?schema=erp&sslmode=require` (url-encoded pw).

---

## 3. Frontends (S3 + CloudFront, serving 200)
| Frontend | CloudFront | Bucket | Prod deploy role |
|---|---|---|---|
| erp (`BNPRS`) | `E2KBCXE3S4W081` = `d393t8cl2frfgi.cloudfront.net` | `nagents-prod-erp-frontend-819144294008` | `gitlab-nagents-prod-erp-frontend-deploy` |
| projects (`BNPRS Sprints`) | `E2U1494OW31PB2` = `d3gkxmmnojgivv.cloudfront.net` | `nagents-prod-projects-frontend-819144294008` | `gitlab-nagents-prod-projects-frontend-deploy` |

- **Prod CI:** each `.web` repo (`bpr0000.bnprs.{erp,projects}.web`) has a **`main` branch** with prod `.gitlab-ci.yml` (prod role/bucket/CF, `AWS_REGION=ap-south-2`, rule on `main`) + prod `.env.production`. Push to `main` → build (`pnpm build`, Vite) → OIDC assume-role → `s3 sync` → CloudFront invalidation. (OIDC roles trust `…web:ref:branch:ref:main`.)
- Build-time env (baked): erp `VITE_API_BASE=https://erp-api.prod.itpgateway.com/api/v2`; projects `VITE_API_URL=https://projects-api.prod.itpgateway.com/api/v1` + `VITE_WS_URL=wss://…/ws`. Both: `VITE_KEYCLOAK_URL=https://accounts-uat.itpgateway.com`, realm `BNPRS-enterprise`, clients `hr-spa` / `projects-frontend`.

---

## 4. Domains / DNS / TLS
- Route53 zone **`itpgateway.com`** = `Z065126021WLLO6U774F7` (in this ITP account).
- ACM wildcard **`*.prod.itpgateway.com`** = `arn:aws:acm:ap-south-2:819144294008:certificate/0247b03d-b021-48f9-9a1e-8d314c4759c8` (DNS-validated) → ALB HTTPS.
- A-alias → prod ALB: `erp-api.prod.itpgateway.com`, `projects-api.prod.itpgateway.com`.
- Frontends on default CloudFront domains (no custom frontend domain yet).

---

## 5. Keycloak — SHARED IdP (nothing deployed)
- Uses shared `utms-keycloak` at `https://accounts-uat.itpgateway.com`, realm `BNPRS-enterprise` (MySQL/Aurora, eu-central-1). nagents does NOT run its own keycloak.
- **🔴 OPEN / BLOCKED (Phase 6) — prod redirect URIs:** the prod frontend origins must be added as Valid Redirect URIs + Web Origins on the `BNPRS-enterprise` clients:
  - `hr-spa` → redirect `https://d393t8cl2frfgi.cloudfront.net/*` , web-origin `https://d393t8cl2frfgi.cloudfront.net`
  - `projects-frontend` → redirect `https://d3gkxmmnojgivv.cloudfront.net/*` , web-origin `https://d3gkxmmnojgivv.cloudfront.net`
  - (APPEND — keep existing UAT entries; add post-logout redirect URIs if used.)
  - **Until done, prod login bounces with redirect_uri mismatch** — the only functional gap left.
  - **BLOCKER (2026-07-26):** could not self-apply — bootstrap admin password in Secrets Manager `utms/prod/keycloak` is INVALID (rotated post-boot); `hr-app-admin` service account is 403 (no `manage-clients`); ECS exec disabled on utms-keycloak; no other admin secret. Needs the utms/shared-keycloak team's current admin creds OR them to make the edit. Request draft: `07-axon-terminals/deliverables/keycloak-prod-redirect-uris-request.md`.
  - **To unblock (any one):** update `utms/prod/keycloak` KEYCLOAK_ADMIN_PASSWORD with the current value; OR grant `hr-app-admin` the `manage-clients` role; OR the shared team adds the 4 entries directly. Then apply via the Admin API.

---

## 6. Open items / gotchas
- **Keycloak redirect URIs** (above) — required for prod login.
- **ECS service replace-on-apply churn:** each `terraform apply` on a backend destroys+recreates the ECS service (downtime + slow) — likely drift from an early CLI `force-new-deployment`; investigate before next apply.
- **Direct-apply stopgap:** future changes should go via CI (portal.iaac `bp_rel` pipeline; `.web` `main`). Real `*.tfvars` are untracked (secrets); values are local/CI-provided.
- **No prod dashboard** frontend; erp `VITE_PORTAL_URL` points at the erp CF domain as a placeholder.
- Single-AZ RDS + OpenVPN off — acceptable UAT-grade prod; harden later.

## 7. Cross-refs
- gitlab agent (na-003/003) mem-024/025/026 (deploy process, shared-keycloak decision, erp Prisma mechanism).
- IaC: `bpr0000.bnprs.portal.iaac` (`nagents-platform/{platform,services}/prod`). App repos: `bpr0000.bnprs.{erp,projects}` (+ `.web`).
