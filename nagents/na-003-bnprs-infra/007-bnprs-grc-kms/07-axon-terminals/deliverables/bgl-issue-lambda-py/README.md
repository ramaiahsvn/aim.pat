# bgl-issue Lambda

Source: `handler.py`. The deployment artifact (`bgl-issue.zip`) is **built on demand**, not
committed — `zip bgl-issue.zip handler.py` — and deployed to Lambda. aim.pat holds no build blobs.
