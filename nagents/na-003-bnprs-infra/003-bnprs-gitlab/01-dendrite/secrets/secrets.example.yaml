# ============================================================
#  AGENT SECRETS — bnprs-gitlab
# ============================================================
#  Copy this file to secrets.yaml and fill in real values.
#  secrets.yaml is git-ignored. NEVER commit real credentials.
#
#  Shared secrets (LLM keys, Slack, storage) live in:
#    ../../../../../../secrets/secrets.yaml   (repo root)
#  Only put agent-specific credentials here.
# ============================================================

# ---- GITLAB API ---- #
gitlab:
  url: "https://gitlab.bnprs.ai"
  api_token: ""   # set via $GITLAB_PAT env var in ~/.zshrc; leave blank here

# ---- EC2 SSH ACCESS ---- #
ssh:
  host: "16.112.21.84"
  user: "ubuntu"
  key_path: "~/.ssh/gitlab-server-key.pem"

# ---- ZOHO MAIL SMTP (msmtp on EC2) ---- #
msmtp:
  smtp_host: "smtp.zoho.com"        # NOTE: smtp.zoho.in does NOT work
  smtp_port: 587
  from: "ramaiah@bnprs.in"
  user: "ramaiah@bnprs.in"
  app_password: ""                  # generate at https://accounts.zoho.in → Security → App Passwords
  config_path: "/etc/msmtprc"       # on EC2 server, permissions 600

# ---- ZOHO WORKDRIVE OAUTH (rclone on EC2) ---- #
zoho_workdrive:
  client_id: "1000.GC6M0Y8P78GM1JTVACRNEHLR11LJOK"
  client_secret: ""                 # stored in /root/.config/rclone/rclone.conf on EC2
  workspace_id: "vz3ifa8cc02e94d60494e8e8f5758ec9752fb"
  backup_folder_id: "vz3if9fdcbf1ad2ee4eafb79d5a8a30c256e7"

# ---- ENVIRONMENT ---- #
env: "production"
