# ============================================================
#  AGENT-SPECIFIC SECRETS — <Agent Name>
# ============================================================
#  Copy this file to secrets.yaml and fill in real values.
#  secrets.yaml is git-ignored. NEVER commit real credentials.
#
#  Shared secrets (LLM keys, Slack, storage) live in:
#    ../../../../../../secrets/secrets.yaml   (repo root)
#  Only put agent-specific credentials here.
# ============================================================

# ---- AWS CREDENTIALS ---- #
# Inherited from 001-bnprs-aws agent via AWS profile 'bnprs'
# No separate credentials needed — use: aws --profile bnprs <command>
aws:
  profile: "bnprs"
  account_id: "891963159778"
  region: "ap-south-2"

# ---- ENVIRONMENT ---- #
env: "production"
