# Long-Term Memory — Persistent Knowledge (LTP)
# Survives across sessions. The agent's accumulated wisdom.

# Format: each entry is a strengthened "synapse"
memories:
  - id: mem-001
    type: project_context
    content: >-
      2026-08-01 THIS AGENT OWNS BIOMETRIC EVALUATION. User decision, taken so that measuring an
      algorithm does not have to disturb na-004/007 cpp-bengine every time.
      THE DIVIDING LINE, and it is worth applying literally:
        cpp-bengine and the cpp-* modality agents own "DOES IT COMPUTE THE RIGHT THING" —
          adapters, the BprID_* ABI, registration, build options, the proof gate.
        THIS AGENT owns "HOW GOOD IS IT" — protocols, datasets, pair lists, scores, thresholds,
          reports, and the numbers themselves.
      THE HANDOFF WHEN EVALUATION FINDS A DEFECT: report the anomaly, DO NOT fix the adapter.
      Route it to the owning agent (na-004/001 cpp-face for BprFace, /002 finger, and so on) and
      re-measure afterwards. That loop ran end to end on 2026-08-01 and caught two real bugs —
      see mem-004 — so it works; keep the roles separate.
      RESULTS LIVE HERE, NOT IN THE ENGINE AGENT'S MEMORY. Numbers get re-measured often and
      would churn cpp-bengine's knowledge base; it keeps a pointer only.
    created_at: "2026-08-01"
    last_accessed: "2026-08-01"
    access_count: 1
    confidence: 1.0
    source: "session-2026-08-01 (user decision)"
    tags: [ownership, evaluation-is-this-agent, report-do-not-fix, results-live-here, na-004-007-handoff]

  - id: mem-002
    type: technical_knowledge
    content: >-
      2026-08-01 THE HARNESS — bengine-eval, in bpr.cpp at
      src/BprIDEngine/bengine/apps/bengine-eval/. THE CODE STAYS THERE because it must link the
      `bengine` facade; THIS AGENT OWNS IT REGARDLESS. Location is not ownership on this platform
      — na-004/001 likewise owns BprFace/ inside the engine tree.
        cmake -S . -B build -DBENGINE_BUILD_FACE_T12=ON [-DBENGINE_BUILD_FACE_T13/T14/T15=ON]
        cmake --build build -j8
        ./build/bengine-eval --data <root> --pairs <pairs.txt> --tid T12|T13|T14|T15 [--pack P]
      Built ONLY when a face variant is enabled, and it links the facade so it discovers which
      --tid values are live rather than being told.
      OPERATIONAL FACTS THAT COST TIME TO REDISCOVER:
      (1) MODELS RESOLVE BESIDE THE BINARY, via BprModelPath (dladdr on the calling module) —
      for an executable that is the BUILD DIRECTORY, so symlink .models/bpr.m1000*.onnx and the
      InspireFace pack into it. Missing models do not fail at startup: adapters load lazily, so
      the symptom is a run of pure extraction failures.
      (2) DO NOT USE --limit FOR A REPORTED NUMBER. The pair list is ordered fold by fold, so a
      limit populates the first folds and empties the rest; the CV degenerates but still prints a
      mean and a std. An early --limit 700 produced "71.5% +/- 21.5". The tool now prints
      "*** PARTIAL RUN — NOT AN LFW RESULT ***" above such figures, and the figures below that
      banner still look plausible — read the banner.
      (3) TWO PAIR FORMATS, sniffed from the header: standard LFW pairs.txt, or "#explicit
      <folds>" followed by "<path-a> <path-b> <same:0|1> <fold>" for any other dataset.
      Generators in apps/bengine-eval/protocols/: lfw_pairs_from_xlsx.py for LFW, and make_pairs.py
      for EVERYTHING ELSE — it treats any directory directly containing images as a subject, keyed
      by FULL RELATIVE PATH. Two things in it are load-bearing rather than tidy:
        - FULL PATH, because dbase numbers subjects 1..n separately under females/ and males/, so
          all 22 female ids also exist as male ids. Keying on the leaf name merges 22 pairs of
          DIFFERENT PEOPLE and manufactures false genuine pairs — which inflates the score and
          looks like nothing went wrong.
        - NATURAL SORT, because subject order sets both the round-robin fold assignment and the
          RNG draw order, so lexicographic (s1, s10, s11, ... s2) silently yields a different,
          irreproducible protocol. With natural sort make_pairs.py reproduces the older
          make_orl_pairs.py output EXACTLY, which is what made deleting that script safe.
      (3b) COMMON-SUBSET MODE, for comparing variants that do NOT share a detector: --fte-list
      <out> writes the image paths a variant could not extract, --exclude <in> skips any pair
      touching a listed image. Two passes: collect per-variant FTE, union, re-run with --exclude
      on the union. Paths only — no image data or embeddings leave the process.
      (4) IT NEVER WRITES EMBEDDINGS OR IMAGES TO DISK, per cpp-face directive 1. Templates are
      cached in memory for the run and the output is aggregate statistics only. DO NOT ADD a
      --dump-features flag if asked — offer an in-process analysis instead.
      (5) 10-FOLD IS DONE PROPERLY: the threshold is fitted on nine folds and applied to the
      tenth. Fitting one threshold on all pairs and reporting its accuracy inflates LFW by around
      half a point and is the commonest way these numbers get overstated.
      STILL MISSING, all of which the nucleus asks for: bootstrap 95% CI on EER (the +/- printed
      today is the ACROSS-FOLD STD, not a CI — do not describe it as one), DET curves, score
      histograms, EER and FMR@FNMR tables. Only TAR@FAR is computed.
    created_at: "2026-08-01"
    last_accessed: "2026-08-01"
    access_count: 1
    confidence: 1.0
    source: "session-2026-08-01"
    tags: [bengine-eval, harness, models-beside-the-binary, never-use-limit, two-pair-formats, no-feature-dump, proper-10fold, std-is-not-a-ci, missing-det-eer]

  - id: mem-003
    type: technical_knowledge
    content: >-
      2026-08-01 FACE BASELINE — all four variants on LFW, ORL, dbase and CroppedYale. Report:
      07-axon-terminals/deliverables/eval-reports/2026-08-01-face-4variant-lfw-orl.md
      Identical protocol and detector settings throughout. FTE: 0 on LFW and ORL; 2 on dbase,
      the SAME 2 for every variant — so all four saw an identical pair set in every case.
      LFW (6000 pairs, 10-fold):
        T14 aFace  99.35% +/- 0.34   TAR@1e-3 98.87%   AdaFace IR-101 / WebFace12M
        T15 mFace  99.33% +/- 0.37   TAR@1e-3 98.80%   MagFace R100 / Glint360K
        T13 iFace  99.02% +/- 0.26   TAR@1e-3 97.37%   InspireFace Pikachu
        T12 sFace  96.60% +/- 0.89   TAR@1e-3 93.50%   OpenCV SFace — THE INCUMBENT
      ORL (3600 pairs, folds split BY SUBJECT):
        T12 99.91% +/- 0.28 | T15 99.80% +/- 0.61 | T14 99.68% +/- 0.95 | T13 99.64% +/- 0.82
      dbase (6994 pairs, 61 subjects, 640x480 colour, folds by subject; same 2 FTE for all four):
        T14 98.64% +/- 3.85  TAR@1e-3 96.83%
        T15 98.60% +/- 3.84  TAR@1e-3 96.68%
        T12 98.39% +/- 3.83  TAR@1e-3 96.36%
        T13 95.08% +/- 3.72  TAR@1e-3 84.39%   <-- COLLAPSES
      THE RANKING INVERTS AND ORL IS THE REASON. T12 is last on LFW and first on ORL — that is
      ORL failing to discriminate, not T12 excelling. All four within 0.27 points, error bars
      0.28-0.95 overlapping completely, three of four at 100% TAR@1e-3. 40 subjects, controlled,
      frontal, single session. ORL IS A SMOKE TEST; DO NOT RANK ALGORITHMS WITH IT. As a smoke
      test it earned its keep: 0 FTE on 92x112 GRAYSCALE PGM tight crops proved the detect/align
      path handles input well outside LFW's shape.
      T14 vs T15 IS A STATISTICAL TIE — 0.02 apart against +/-0.35 bars. Ranking them is noise.
      READ TAR@FAR, NOT ACCURACY. T14/T15 ~98.8% against T13's 97.4% at FAR=1e-3 means iFace
      rejects roughly 2.4x as many genuine users at a fixed 1-in-1000 false-accept rate; the
      accuracy column understates that gap by more than half.
      OPEN, ROUTED TO na-004/001 cpp-face: T12's 2.8-point deficit looks like INTEGRATION rather
      than the model. It is the ONLY variant whose score depends on external pre-alignment (+0.77
      from lfw-deepfunneled, where T13/T14/T15 are -0.07/-0.09/-0.07) and its fold variance is 3x
      the others. Published SFace is ~99.4%. Those points are on the ONLY SHIPPABLE VARIANT,
      which makes them worth more than anything the licence-blocked ones can offer.
      CROPPEDYALE (38 subjects x ~64 illumination directions, 12924 pairs on a COMMON SUBSET —
      see below): THE ONLY SET THAT GENUINELY STRESSES THESE MODELS.
        T13 iFace  98.27% +/- 0.56   TAR@1e-2 97.28%   TAR@1e-3 81.74%
        T14 aFace  97.69% +/- 1.11   TAR@1e-2 96.53%   TAR@1e-3 93.63%
        T15 mFace  96.54% +/- 0.98   TAR@1e-2 93.99%   TAR@1e-3 89.56%
        T12 sFace  89.92% +/- 2.30   TAR@1e-2 74.75%   TAR@1e-3 58.04%
      THREE THINGS ONLY THIS DATASET COULD SHOW:
      (1) T12 COLLAPSES — TAR@1e-3 of 58% means ~42% OF GENUINE USERS REJECTED at a 1-in-1000
      false-accept rate, against T14's 93.6%. On LFW it trailed by 2.8 points; here by 36.
      Whatever ails T12 costs far more on hard imagery than LFW implied.
      (2) T13 AND T14 CROSS OVER BETWEEN OPERATING POINTS, and this is the single best argument
      for reading TAR@FAR rather than accuracy. T13 has the BEST accuracy (98.27%) and the best
      TAR@1e-2 (97.28%), but tightening to FAR=1e-3 costs it 15.5 points while T14 loses only
      2.9. T13 has a HEAVIER IMPOSTOR TAIL — it separates the bulk well but has more extreme
      false-accept scores. FOR AUTHENTICATION, WHICH LIVES AT STRICT FAR, T14 IS 12 POINTS BETTER
      THAN THE VARIANT THAT "WINS" ON ACCURACY.
      (3) ADAFACE FINALLY SEPARATES FROM MAGFACE. Tied on LFW (0.02), ORL and dbase; here T14
      leads T15 by 1.15 accuracy and 4.1 on TAR@1e-3 — the direction the -CCTV naming predicts,
      since a quality-adaptive margin should pay off exactly where quality varies. First evidence
      in this evaluation for preferring one over the other.
      HOW THE COMMON SUBSET WAS BUILT, AND WHY IT WAS NECESSARY: first-pass FTE differed by
      variant (T13 388, others 418) because T13 uses InspireFace's OWN detector, not YuNet, so it
      survives images the others drop. Different FTE = different pair sets = not a comparison
      (directive 5 caught this). Collect each variant's failures with --fte-list, union them (481
      images), re-run all with --exclude on the union -> 12924 identical pairs. USE THIS TWO-PASS
      PATTERN WHENEVER VARIANTS DO NOT SHARE A DETECTOR.
      DBASE IS THE FIRST SET THAT SEPARATED ANYTHING, AND IT SEPARATED T13 DOWNWARDS. iFace goes
      99.02% (LFW) -> 95.08%, and its TAR@1e-3 collapses 97.37% -> 84.39% — about 1 genuine user
      in 6 rejected at a 1-in-1000 false-accept rate, against ~1 in 30 for the other three, which
      finish within 0.25 points of each other. Likely cause: Pikachu is InspireFace's LIGHTWEIGHT
      EDGE pack, and a small model degrades faster than IR-101 once the data stops being frontal
      and clean. SO THIS IS A RESULT ABOUT THE PACK, NOT NECESSARILY ABOUT INSPIREFACE — evaluate
      the Megatron pack before drawing any conclusion about the SDK.
      READ DBASE'S +/-3.8 BEFORE RANKING ON IT. That bar is ~10x LFW's and nearly identical across
      all four variants (3.72-3.85), which means it is driven by WHICH SUBJECTS LANDED IN WHICH
      FOLD, not by the models — 61 subjects over 10 folds is ~6 per fold, so one hard identity
      moves a whole fold. The 0.25-point spread between T12/T14/T15 is far inside it and means
      NOTHING. T13's ~3.5-point deficit is around one std, so on accuracy alone it is only
      suggestive; the TAR@FAR gap is what makes it decisive, because TAR is computed over the
      POOLED score set rather than per fold. GENERAL: on a small-subject database, trust TAR@FAR
      over fold-averaged accuracy.
      NOT QUOTABLE EXTERNALLY: T13/T14/T15 all use research-licensed weights with no commercial
      grant (na-004/007 mem-030, mem-032). Measurements, not options.
    created_at: "2026-08-01"
    last_accessed: "2026-08-01"
    access_count: 1
    confidence: 1.0
    source: "session-2026-08-01 (bengine-eval, engine bpr.cpp 9e412ff)"
    tags: [face-baseline, lfw, orl, ranking-inverts, orl-is-a-smoke-test, t14-t15-tie, read-tar-at-far, t12-integration-gap, not-quotable]

  - id: mem-004
    type: incident_learning
    content: >-
      2026-08-01 EVALUATION FOUND TWO DEFECTS NOTHING ELSE COULD HAVE — the argument for this
      agent existing. Both were invisible to a clean build, 59 passing unit tests and a green
      proof gate. Reported from here, fixed by na-004/007 (bpr.cpp e2a16b0, 9e412ff).
      (1) MIRRORED ALIGNMENT in T14/T15 — 63.16% -> 99.49% once corrected. The ArcFace 5-point
      template's first point sits at x=38 (IMAGE LEFT) and YuNet's "right eye" is the SUBJECT's
      right eye, which appears image-left: they already correspond, and the adapter swapped them.
      IT FAILED SILENTLY — detection succeeded, crops were face-shaped, embeddings were 512
      finite floats, and a photo still SELF-MATCHED at 1.0. Only the relationship BETWEEN
      different images of one person was destroyed, and no unit test looks at that.
      63% IS THE DANGEROUS SHAPE OF WRONG: not 50% (obviously broken), not 99% (obviously fine) —
      near chance but easy to explain away as "CPU model" or "hard dataset". WHEN A VARIANT LANDS
      NEAR CHANCE, SUSPECT GEOMETRY BEFORE THE MODEL. Cheap discriminator for a mirrored
      alignment: a photo must self-match ~1.0 while a MIRRORED COPY scores clearly LOWER. If both
      score the same, the landmark order is wrong.
      (2) DETECTOR THRESHOLD MISMATCH — the T14/T15 path used YuNet's default 0.9 score threshold
      where T12 uses 0.6, discarding 70 faces T12 accepted. Not merely a 0.9% FTE: it meant the
      two groups were scored on DIFFERENT PAIR SETS, because a failed pair leaves both numerator
      and denominator. After the fix, FTE 70 -> 0 and accuracy moved within the error bars.
      THE RULE THIS GIVES THE AGENT: BEFORE COMPARING VARIANTS, VERIFY THEY SAW THE SAME DATA.
      Equal pair counts and equal FTE are the check. A comparison across different effective test
      sets is not a comparison, and an FTE difference is the usual way that happens unnoticed.
    created_at: "2026-08-01"
    last_accessed: "2026-08-01"
    access_count: 1
    confidence: 1.0
    source: "session-2026-08-01"
    tags: [evaluation-finds-what-tests-cannot, mirrored-alignment, near-chance-suspect-geometry, mirror-test, detector-threshold, verify-same-data-before-comparing, fte-parity]

  - id: mem-005
    type: technical_knowledge
    content: >-
      2026-08-01 MULTI-ALGORITHM FUSION AND TIMING — the two axes the first sixteen runs missed.
      Both prompted by the user asking; neither had occurred to me, and both changed conclusions.
      FUSION ON CROPPEDYALE, identical 12924 pairs, all figures comparable to the singles:
        T13+T14 mean  98.75% +/- 0.68  TAR@1e-2 98.60%  TAR@1e-3 96.27%  <-- BEST MEASURED ANYWHERE
        T13+T14 max   98.86% +/- 0.57  TAR@1e-2 98.85%  TAR@1e-3 93.13%
        all four mean 97.75% +/- 0.97  TAR@1e-2 96.58%  TAR@1e-3 91.70%
        T14+T15 mean  97.46% +/- 1.10  TAR@1e-2 96.19%  TAR@1e-3 92.85%
        T12+T14 mean  96.32% +/- 1.46  TAR@1e-2 93.55%  TAR@1e-3 88.17%
        (singles: T13 98.27/81.74, T14 97.69/93.63, T15 96.54/89.56, T12 89.92/58.04)
      FOUR RULES THAT GENERALISE, and the first is the only one people usually know:
      (1) FUSION PAYS ONLY WHEN THE COMPONENTS FAIL DIFFERENTLY. T13+T14 beats BOTH on all three
      metrics — TAR@1e-3 96.27% against T13's 81.74% (+14.5) and T14's 93.63% (+2.6). The
      predictor was the CROSSOVER: T13 won at FAR=1e-2 and T14 at 1e-3, which is direct evidence
      they misclassify different faces. LOOK FOR A CROSSOVER BEFORE SPENDING ON FUSION.
      (2) FUSING SIMILAR MODELS HURTS. T14+T15 (both IR-101, similar training data) came out
      WORSE than T14 alone, 97.46 vs 97.69. No independent information, and the weaker one drags.
      (3) FUSION CANNOT RESCUE A WEAK VARIANT. T12+T14 lost 5.5 points of TAR@1e-3 against T14
      alone. Averaging pulls the strong component DOWN; it does not lift the weak one up.
      (4) MORE IS NOT BETTER. All four fused is worse than the best pair on every metric. Choose
      COMPLEMENTARY components, not many.
      MAX vs MEAN SPLITS BY OPERATING POINT, mirroring the crossover exactly: max takes the
      highest score so it lifts genuine AND impostor scores — best at FAR=1e-2 (98.85%), worse at
      1e-3 (93.13%). mean damps impostor spikes and wins where authentication lives. USE MEAN FOR
      STRICT FAR, MAX FOR SCREENING.
      TIMING (dbase 640x480, single-threaded, per call):
        T12  8.57 ms extract | 12.73 us match | first call 47.5 ms
        T13  9.83 ms extract |  0.48 us match | first call 15.2 ms
        T14 35.04 ms extract |  0.65 us match | first call 202.9 ms
        T15 35.25 ms extract |  0.67 us match | first call 197.0 ms
      THIS REFRAMES THE ACCURACY WORK AND THE STANDING RECOMMENDATION. T14/T15 cost 4x T12's
      extraction — IR-101 (260 MB) against SFace (37 MB). T12 is not merely the only SHIPPABLE
      variant, it is also the CHEAPEST, so closing its integration gap is worth more than the
      accuracy table alone implied. Conversely the best configuration measured anywhere,
      T13+T14, costs ~45 ms per image AND cannot ship (both licence-blocked) — an upper bound,
      not a proposal.
      MATCHING IS EFFECTIVELY FREE for the embedding variants: sub-microsecond, so a 100k-entry
      gallery adds ~50 ms to a 35 ms extraction. GALLERY SIZE IS NOT THE BOTTLENECK, ENROLMENT
      IS. T12's 12.7 us is 26x the others (it round-trips OpenCV's matcher rather than a plain
      cosine) and still irrelevant at any realistic scale.
      HOW TO MEASURE IT HONESTLY — two choices in bengine-eval worth preserving: the FIRST
      extraction per variant is excluded from the mean and reported separately, because it pays
      the lazy model load and folding it in would libel whichever variant has the largest model;
      and CACHE HITS ARE NOT TIMED, or the average improves with dataset redundancy rather than
      with the algorithm. Note the first-call figure is load PLUS one inference, so T14's true
      load is ~203-35 = ~168 ms.
      FUSED RUNS SCORE A PAIR ONLY IF EVERY LISTED VARIANT EXTRACTED BOTH IMAGES, so a fused run
      and its components share a pair set. Without that, fusion flatters itself by dropping
      whatever any one component found hard.
    created_at: "2026-08-01"
    last_accessed: "2026-08-01"
    access_count: 1
    confidence: 1.0
    source: "session-2026-08-01 (user asked; both axes had been missed)"
    tags: [fusion, timing, crossover-predicts-fusion-gain, similar-models-do-not-fuse, fusion-cannot-rescue-a-weak-variant, more-is-not-better, mean-for-strict-far-max-for-screening, t12-is-4x-cheaper, matching-is-free, enrolment-is-the-bottleneck, exclude-first-call, do-not-time-cache-hits]

# Pruning rules
pruning:
  max_entries: 500
  stale_after_days: 90
  min_confidence: 0.3
  strategy: "least_recently_used"
